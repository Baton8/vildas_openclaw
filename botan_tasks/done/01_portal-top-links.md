# タスク: トップページにツールリンク集を追加

## 概要
`sites/portal/public/` 以下のディレクトリを自動スキャンして、
トップページにリンク一覧として表示する。

## 実装方針
- `app/page.tsx` でサーバーサイドから `public/` をスキャン
- 各ディレクトリ内の `meta.json` があれば title/description/icon を使用
- なければディレクトリ名をそのまま表示

## 完了条件
- トップページに「ツール」セクションが表示される
- quantum-gomoku がリンクとして表示される
- ビルド通過・git push済み
